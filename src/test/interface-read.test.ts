import { describe, expect, it } from 'vitest';

// Skip these tests in CI - they require /tmp/calculator.bin
describe.skip('Interface Schema Reading', () => {
  it('placeholder', () => {
    expect(true).toBe(true);
  });
});
